# 贡献指南

感谢你对 **hsx-dsh-tools** 的关注！本仓库由作者个人维护，以下约定能让双方都省心。

## 一句话约定

- ✅ **欢迎**：提建议、报告问题、讨论使用场景。
- ❌ **暂不接受外部 Pull Request（PR）**。

这是个人项目，作者希望保持界面风格、交互与发布节奏的一致性，因此**不会接受任何外部提交的代码**。即便如此，你贡献的想法仍然很有价值——请通过 Issue 提出。

## 如何提建议或报告问题

1. **先搜索**：查看是否已有相同或相近的 Issue，避免重复。
2. **开启一个 Issue**，并尽量提供：
   - 问题描述（你期望什么、实际发生了什么）；
   - 复现步骤；
   - 系统环境（Windows 10 / 11、x64）；
   - 涉及的具体 exe（安装 / 启动 / 升级）与报错输出、日志截图；
   - 版本号（可通过 `exe --version` 查看）。

## 关于 Pull Request

本项目**不接受外部 Pull Request**：直接提交的 PR 会被关闭且不会合并，请谅解。

## 开发环境

构建、目录结构、测试与发版说明见 [docs/DEVELOPMENT.md](./docs/DEVELOPMENT.md)。代码使用 Go 1.24，默认 vendor 模式。

## 开源许可

本项目采用 [MIT 许可证](./LICENSE) 授权。
